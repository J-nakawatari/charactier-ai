name: E2E Tests

on:
  # 手動実行
  workflow_dispatch:
    inputs:
      browsers:
        description: 'ブラウザ選択'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - chromium
          - firefox
          - webkit
  
  # プルリクエスト時は自動実行（オプション）
  pull_request:
    types: [opened, synchronize]
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      LANG: ja_JP.UTF-8
      LC_ALL: ja_JP.UTF-8
    services:
      mongodb:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      mailhog:
        image: mailhog/mailhog
        ports: ['1025:1025', '8025:8025']
      # stripe:
      #   image: stripe/stripe-cli
      #   ports: ['12111:12111']
      # minio:
      #   image: minio/minio:latest
      #   env:
      #     MINIO_ROOT_USER: minio
      #     MINIO_ROOT_PASSWORD: minio123
      #   ports: 
      #     - 9000:9000
      #   options: >-
      #     --health-cmd "curl -f http://localhost:9000/minio/health/live || exit 1"
      #     --health-interval 10s
      #     --health-timeout 5s
      #     --health-retries 5
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
          
      - name: Install root dependencies
        run: pnpm install --no-frozen-lockfile
        
      - name: Install frontend dependencies
        run: |
          cd frontend
          pnpm install --no-frozen-lockfile
          
      - name: Install backend dependencies
        run: |
          cd backend
          pnpm install --no-frozen-lockfile
        
      - name: Install Playwright browsers and dependencies
        run: |
          # システムパッケージを更新
          sudo apt-get update
          # Playwrightの依存関係をインストール
          sudo npx playwright install-deps chromium firefox webkit
          # ブラウザをインストール
          npx playwright install chromium firefox webkit
      
      - name: Build frontend
        run: |
          cd frontend
          npm run build
      
      - name: Start servers
        run: |
          # 環境変数を設定
          export NODE_ENV=test
          export JWT_SECRET=test-secret-key
          export MONGO_URI=mongodb://localhost:27017/test
          export SENDGRID_API_KEY=dummy-key-for-test
          export STRIPE_SECRET_KEY=sk_test_dummy
          export STRIPE_WEBHOOK_SECRET=whsec_test_dummy
          
          # バックエンドサーバーを起動
          cd backend && NODE_ENV=test MONGO_URI=mongodb://localhost:27017/test JWT_SECRET=test-secret-key JWT_REFRESH_SECRET=test-refresh-secret FRONTEND_URL=http://localhost:3000 SMTP_HOST=localhost SMTP_PORT=1025 npm run dev > backend.log 2>&1 &
          BACKEND_PID=$!
          sleep 10
          
          # フロントエンドサーバーを起動
          cd frontend && npm run start > frontend.log 2>&1 &
          FRONTEND_PID=$!
          sleep 15
          
          # サーバーが起動したか確認
          echo "=== Checking server status ==="
          curl -f http://localhost:5000/api/v1/health || (echo "Backend not ready"; cat backend/backend.log | tail -50; exit 1)
          curl -f http://localhost:3000 || (echo "Frontend not ready"; cat frontend/frontend.log | tail -50; exit 1)
        
      - name: Run E2E tests
        env:
          PLAYWRIGHT_BASE_URL: http://localhost:3000
        run: |
          if [ "${{ github.event.inputs.browsers }}" = "all" ]; then
            npm run test:e2e
          else
            npm run test:e2e -- --project=${{ github.event.inputs.browsers }}
          fi
        
      - name: Generate failure report
        run: |
          npm run failures:md || echo "No test results found"
        if: always()
        
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: |
            playwright-report
            coverage/playwright-results.json
            coverage/failures.md